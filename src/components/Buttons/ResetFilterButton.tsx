import React from 'react'
import { StyledButton } from '../../styles'

export const ResetFilterButton = () => {
    return (
        <StyledButton
            color='#FF0000'
            bc={'rgba(255, 0, 0, 0.20)'}
        >
            Сбрость фильтры
        </StyledButton>
    )
}
